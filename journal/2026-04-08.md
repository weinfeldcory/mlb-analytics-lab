# 2026-04-08

## Session
Worked on rebuilding and iterating the hitter projection engine, then pushed it into a usable local workflow. Added the 2025 hitter model tables and exports, rebuilt the main hitter engine with cleaner playing-time and skill layers, improved the WAR-style value stack, tested archetype, position, catcher, and elite-power adjustments, and exported the current model outputs for inspection.

Built a local browser viewer so the model can be explored like the original spreadsheet. Added team filtering, search, sortable outputs, team snapshot cards, and a more compact UI, then fixed the team filter logic so it prefers the current season team instead of stale prior-team assignments.

Added a live MLB StatsAPI pipeline for completed games, player boxscore lines, and current rosters. Used that to create a 2026 comparison export that shows preseason projections next to current season-to-date stats and 162-game pace in the local viewer.

Summary: Built the hitter model workflow, local viewer, live data pull, and 2026 projection-vs-current comparison.
Branch: `main`
Changes before commit: updated the repo docs and modeling scripts, added new SQL tables and exports, added a live StatsAPI pull script, created the local viewer, and generated fresh data outputs under `data/`.
