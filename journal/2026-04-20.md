# 2026-04-20

## Session
Worked on two product slices in the sports-games-and-analytics workspace. In `projects/mlb-attendance-app`, continued the onboarding and local-persistence flow and tightened reset and import behavior so the app does not bounce users back through first-run setup after valid state changes. In `projects/nfl-pick-em-game`, corrected the scoring model so weekly reconciliation matches the actual pool structure, including multi-winner POTD handling, per-game totals outcomes, auto-derivation from posted results, and manual commissioner overrides.

Summary: Checkpoint MLB attendance onboarding/persistence work and NFL pick'em scoring reconciliation.
Branch: `main`
Changes before commit: updated `projects/mlb-attendance-app` onboarding, profile, storage, and domain files; updated `projects/nfl-pick-em-game` scoring, store, UI, and tests; added this journal entry.

## Session
Finished the latest MLB attendance app polish pass. Corrected the Home tab so home runs seen are derived from real batting lines instead of sparse witnessed-event tags, changed the attendance pattern graphic to group exact first-pitch times in Eastern Time, and made the Home teams-seen table sortable from the page itself.

Summary: Finalize MLB attendance home tab stats, time buckets, and sortable team table.
Branch: `main`
Changes before commit: modified `projects/mlb-attendance-app/apps/mobile/src/features/home/HomeScreen.tsx`; modified `projects/mlb-attendance-app/packages/domain/src/stats/calculatePersonalStats.ts`.

## Session
Continued the `march-madness-game` app against the product roadmap by replacing the prototype-style long-scroll shell with distinct product surfaces. Added a real `Overview` view, mobile-friendly top-level navigation, a dedicated `Commissioner` surface, and shared standings rendering so the app now maps more cleanly to the intended `Overview`, `Draft Room`, `Standings`, `Teams`, `Scoring Lab`, and `Commissioner` structure.

Summary: Restructure March Madness app shell around product views and overview.
Branch: `main`
Changes before commit: modified `projects/mlb-attendance-app/apps/mobile/src/features/home/HomeScreen.tsx`; modified `projects/mlb-attendance-app/packages/domain/src/stats/calculatePersonalStats.ts`; modified `journal/2026-04-20.md`; modified `/Users/coryweinfeld/march-madness-game/index.html`, `src/main.js`, `src/styles.css`, `src/ui/analytics.js`, and `src/ui/state.js`; left `/Users/coryweinfeld/march-madness-game/data/season-state.json` uncommitted.
